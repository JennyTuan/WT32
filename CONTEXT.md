# CONTEXT.md — CT 领域知识库

> 本文件面向 AI 编程助手，补充医疗 CT 领域知识。  
> AI 不熟悉放射医学，本文件定义了项目中涉及的所有专业术语、参数含义与临床背景，  
> 确保 AI 在生成代码、文案、UI 标签时不犯医疗领域错误。

**严谨性边界**：
- 本项目是 CT 控制端原型，不是临床诊疗系统、剂量计算系统或真实设备控制软件。
- 本文件中的数值范围用于 UI 原型、参数校验和文案约束；不能替代厂商设备手册、医院协议、医学物理师审核或法规要求。
- CT 参数高度依赖设备型号、探测器排数、重建算法、患者体型、临床适应证和本地协议；凡涉及剂量、对比剂、安全阈值的文案，必须使用“预计值 / 参考值 / 需确认”等措辞。
- AI 不应生成诊断结论、治疗建议、真实剂量承诺或“保证安全”的表述。

---

## 一、CT 基础原理

### CT 是什么

CT（Computed Tomography，计算机断层扫描）是一种利用 X 射线从多个角度对人体进行扫描，再由计算机重建出横断面图像的医学成像技术。

**核心组件**：
- **X 射线球管（Tube）**：发射 X 射线的射线源，安装在机架（Gantry）上旋转
- **探测器（Detector）**：接收穿过人体后的 X 射线，转换为电信号
- **机架（Gantry）**：球管和探测器旋转的圆形结构，患者通过中间的孔洞
- **检查床（Table / Couch）**：承载患者，可在扫描孔内前后移动
- **操作控制台（Operator Console）**：技师操控 CT 参数和流程的软件界面（即本项目模拟的对象）

### 扫描过程简述

1. 患者躺在检查床上，进入机架扫描孔
2. 机架旋转，球管和探测器围绕患者旋转并采集数据
3. 计算机将采集的原始数据（Raw Data）重建为横断面图像
4. 技师在控制台查看图像，确认质量

---

## 二、扫描类型与模式

### 2.1 定位像（Scout / Topogram / Scanogram）

**定义**：扫描正式序列前，先进行的一次低剂量扫描，获得患者的正位（AP）或侧位（Lateral）投影图像，类似于普通 X 光平片。

**用途**：
- 确定扫描范围（起止位置）
- 观察患者体位是否正确
- 确认扫描中心

**项目对应**：
- 代码中：`topogram` / `scout`
- 路由：`/protocol-detail/scout`、`/scout-scan`、`/scout-execute`
- 数据模型：`TopogramParam`、`ScanSessionTopogramParam`

**关键参数**：
- 扫描方向：AP（前后）或 Lateral（侧位）
- 管电压（kV）：通常比正式序列低
- 管电流（mA）：固定值，通常 10~50 mA
- 扫描范围（mm）

### 2.2 螺旋扫描（Helical Scan / Spiral Scan）

**定义**：机架持续旋转的同时，检查床匀速前进，球管轨迹呈螺旋形，是现代 CT 最常用的扫描方式。

**优点**：
- 扫描速度快，减少运动伪影
- 可实现大范围连续扫描
- 可进行多期增强（动脉期/静脉期/平衡期）

**项目对应**：
- 代码中：`helical`
- 路由：`/protocol-detail/helical`、`/helical-confirm`、`/helical-execute`
- 数据模型：`HelicalParam`、`ScanSessionHelicalParam`

### 2.3 轴扫（Axial Scan / Step-and-Shoot）

**定义**：检查床固定，机架旋转一圈采集一层（或多层）数据，然后床移动到下一个位置，重复采集。旧式扫描方式，目前主要用于特定部位（如内耳、颅底等需要高分辨率的检查）。

**项目对应**：
- 代码中：`axial`
- 数据模型：`AxialParam`、`ScanSessionAxialParam`

### 2.4 4D 呼吸门控扫描（4D Respiratory Gating / 4D CT）

**定义**：在采集 CT 图像的同时，同步记录患者的呼吸信号（呼吸波形）。根据呼吸周期将图像分配到不同的时相（Phase），获得代表不同呼吸状态的图像组。

**第四维度**：时间（呼吸周期的不同时相），前三维是空间（X、Y、Z）。

**主要用途**：
- 胸腹部肿瘤放疗计划（评估肿瘤随呼吸的运动范围）
- 肺功能评估
- 心脏/腹部 4D 成像

**呼吸时相（Phase）**：
- 常见实现会分为 0%~90% 共 10 个时相；0%/50% 对应吸气末或呼气末的定义可能随厂商、外部呼吸监测系统和项目约定而不同
- 每个时相对应一套完整的断面图像
- 若项目采用“0% = 吸气末、50% ≈ 呼气末”的演示约定，应在代码和 UI 中保持一致，不要把它写成所有 4D CT 系统的通用事实
- 吸气末：肺体积通常较大；呼气末：肺体积通常较小。具体病灶位移方向取决于解剖位置，不能简单固定为“最高/最低位”

**呼吸波形（Breathing Waveform）**：
- 通过体表标记（如腹部压力传感器、红外标记）实时记录呼吸运动
- 波形的峰值（Peak）对应吸气末，谷值（Valley）对应呼气末
- 技师可在波形上调整峰谷识别点，决定时相分割方式

**4D 扫描流程（项目实现）**：
```
诊断扫描确认（FourDDiagnosticConfirmScreen）
→ 呼吸训练（让患者练习均匀呼吸）
→ 正式 4D 采集（WebSocket 实时呼吸波形）
→ 时相回顾（FourDPhaseReviewScreen）：查看各时相图像质量
→ 重扫选择（FourDRescanSelectScreen）：对质量差的时相选择重扫
→ 图像查看（ViewScreen）
```

**项目关键代码**：
- `acquisition_type === "4d"` — 唯一的 4D 判断条件
- `lib/fourDTypes.ts` — 4D 相关类型（BedPhaseCell、PhaseSelections 等）
- `lib/fourDDicomSource.ts`、`lib/fourDImageSource.ts`

---

## 三、核心扫描参数详解

### 3.1 管电压（Tube Voltage / kV）

**定义**：X 射线球管两端的电压差，决定 X 射线的能量（穿透力）。

**常用值**：80 kV / 100 kV / 120 kV / 140 kV

**临床意义**：
- kV 越高，X 射线穿透力越强，适合体型较大患者
- kV 越低，对碘对比剂的显示效果更好（更高的碘对比度），但噪声增加
- 儿童、消瘦患者通常使用低 kV（80~100 kV）以降低剂量
- 胸部/腹部大体型成人常用 120 kV

**代码字段**：`kv`

### 3.2 管电流（Tube Current / mA）

**定义**：X 射线球管的电流强度，决定 X 射线的数量（光子数）。

**相关参数**：
- `mA`：手动固定管电流值
- `mAs`（毫安秒）：mA × 旋转时间，表示每层的总曝光量
- `auto_ma`：自动管电流调制（AEC，Automatic Exposure Control）
- `min_ma` / `max_ma`：自动 mA 的调制范围下限/上限

**自动管电流调制（Auto mA / AEC）**：
根据患者体型和扫描部位自动调整 mA，在保证图像质量的前提下降低剂量。技师设置 mA 范围（min_ma ~ max_ma），系统在范围内自动选择。

**临床参考范围**：
- 定位像：10~50 mA
- 螺旋扫描：100~400 mA（根据部位和体型）
- 儿童协议：需严格控制，不应使用成人默认值

**代码字段**：`auto_ma`、`min_ma`（`ma_min`）、`max_ma`（`ma_max`）

### 3.3 螺距（Pitch）

**定义**：螺旋扫描中，机架旋转一圈时检查床前进的距离，除以探测器宽度。

**公式**：`Pitch = 床速(mm/转) ÷ 探测器宽度(mm)`

**典型值**：0.5 ~ 1.5

**临床意义**：
- Pitch < 1：图像有重叠，辐射剂量较高，但图像质量更好
- Pitch = 1：标准覆盖，无重叠
- Pitch > 1：快速扫描，减少运动伪影，但剂量降低，覆盖可能有轻微缺口
- 心脏 CT 常用低 Pitch（0.2~0.4）以获得高时间分辨率
- 胸腹部常用 Pitch 0.8~1.2

**代码字段**：`pitch`

### 3.4 机架旋转时间（Gantry Rotation Time / Rotation Time）

**定义**：机架（球管+探测器）旋转一圈所需的时间（秒）。

**典型值**：0.25s / 0.33s / 0.5s / 0.75s / 1.0s

**临床意义**：
- 旋转时间越短，时间分辨率越高，对运动器官（心脏）效果越好
- 旋转时间越短，mA 相同时总曝光量（mAs）越少，需要适当提高 mA 补偿
- 心脏 CT 要求最短旋转时间（≤0.35s）

**代码字段**：`rotation_time`

### 3.5 扫描视野（Field of View / FOV）

**定义**：CT 重建图像所覆盖的圆形区域直径（mm）。

**分类**：
- **扫描 FOV（Scan FOV）**：实际扫描时的采集范围
- **重建 FOV（Recon FOV / Display FOV）**：图像重建时使用的视野，可以比扫描 FOV 小（局部放大）

**典型值**：160~500 mm，常用 200~380 mm

**临床意义**：
- 脑部：约 200~250 mm
- 胸部：约 350~380 mm
- 腹部：约 380~500 mm
- FOV 越小，相同矩阵下空间分辨率越高

**代码字段**：`fov`

### 3.6 层厚（Slice Thickness）

**定义**：每层 CT 图像在 Z 轴（体轴方向）的厚度（mm）。

**典型值**：0.5 mm / 1 mm / 2 mm / 3 mm / 5 mm / 10 mm

**临床意义**：
- 层厚越薄，Z 轴分辨率越高，可以看到更细小的病变，但噪声增加
- 层厚越厚，信噪比越好，但小病变可能被"平均"掉（部分容积效应）
- 高分辨率重建（肺、骨）：0.5~1 mm
- 常规腹部扫描：2~5 mm
- 定位像不涉及层厚概念

**代码字段**：`slice_thickness`

### 3.7 层间距（Slice Interval）

**定义**：相邻两层图像中心之间的距离（mm）。

**与层厚的关系**：
- 层间距 = 层厚：无重叠（最常见）
- 层间距 < 层厚：有重叠（用于 MPR/三维重建）
- 层间距 > 层厚：有间隔（不推荐，会漏掉病变）

**代码字段**：`slice_interval`

### 3.8 参数校验原则（原型系统必须遵守）

CT 扫描参数不是普通表单字段。即使本项目不连接真实设备，也必须在前端和后端都限制输入范围，避免生成不符合临床常识的协议。

**推荐校验策略**：
- 使用白名单枚举优先于自由输入：如 kV、kernel、matrix、scan_mode、series_type。
- 对连续数值设置上下限和步进：如 mA、pitch、rotation_time、scan_length、slice_thickness、fov。
- 儿童/婴幼儿协议不能复用成人剂量默认值；`age_group = child | infant` 时，应显示更明显的剂量确认和更保守的默认范围。
- 修改会话参数时只影响 `ScanSession*` 副本，不能反写协议模板，除非用户明确选择“保存为模板”。
- 剂量字段缺失时，UI 应显示“未估算 / 待计算”，不要静默显示 0。0 mGy 或 0 mGy·cm 容易被误解为无剂量。

**原型级参考范围**（需按项目协议再细化）：

| 参数 | 常见原型范围 | UI/校验注意 |
|------|-------------|------------|
| kV | 70~150 kV；常用离散值 80/100/120/140 | 优先做枚举，不建议任意输入 |
| 定位像 mA | 10~80 mA | 定位像低剂量，但不是“无剂量” |
| 诊断扫描 mA | 20~800 mA | 受设备上限、体型、部位影响很大 |
| Pitch | 0.2~2.0 | 心脏、4D、特殊协议可能不同 |
| Rotation Time | 0.25~2.0 s | 与运动伪影、mAs、设备能力相关 |
| Scan Length | 10~2000 mm | 必须与定位像和解剖部位一致 |
| FOV | 50~500 mm | 儿童和局部高分辨率检查更小 |
| Slice Thickness | 0.5~10 mm | 薄层利于重建但噪声更高 |

以上范围只用于防止明显错误输入，不代表真实设备许可范围。

---

## 四、图像重建参数

### 4.1 重建滤波函数（Reconstruction Kernel / Filter Kernel）

**定义**：CT 图像重建时使用的数学滤波算法，决定图像的锐利度和噪声水平。

**项目中的 Kernel 选项**：

| Kernel | 中文名 | 用途 | 特点 |
|--------|--------|------|------|
| Brain2 | 脑部标准重建 | 头颅、脑部软组织 | 平滑，噪声低，适合软组织分辨 |
| Lung2 | 肺部高分辨率重建 | 肺实质、气道、间质病变 | 锐利，噪声高，显示肺细节 |
| Bone2 | 骨骼重建 | 骨皮质、骨小梁 | 最锐利，噪声最高，不适合软组织 |
| S2 | 软组织标准重建 | 腹部、盆腔、颈部软组织 | 平滑偏软，低噪声 |
| S3 | 软组织增强重建 | 腹部、血管 | 中等锐利，平衡噪声与细节 |

**注意**：不同厂家的 Kernel 命名不同（如 Siemens 用字母+数字，GE 用不同名称）。本项目采用的是简化命名。

**代码字段**：`kernel`

### 4.2 重建矩阵（Matrix）

**定义**：图像的像素网格大小，通常为 512×512。

**典型值**：512×512（标准），有时用 1024×1024（超高分辨率）

**代码字段**：`matrix`

### 4.3 窗宽（Window Width / WW）

**定义**：DICOM 图像显示时，映射为灰度范围的 CT 值（HU）跨度。

**作用**：决定图像的对比度。窗宽越窄，对比度越高；窗宽越宽，层次越丰富。

**临床预设**：

| 组织 | 窗宽 WW | 窗位 WL |
|------|---------|---------|
| 脑组织（软组织窗） | 80 HU | 40 HU |
| 骨窗 | 1500~2000 HU | 300~500 HU |
| 肺窗 | 1500~1800 HU | -600 HU |
| 腹部软组织窗 | 300~400 HU | 40~60 HU |
| 纵隔窗 | 350~450 HU | 20~40 HU |

**代码字段**：`window_width`

### 4.4 窗位（Window Level / WL / Window Center）

**定义**：DICOM 图像显示时，灰度范围的中心 CT 值（HU）。

**作用**：决定显示哪个密度范围的组织。窗位越高，越亮的组织（骨骼）被显示为灰；窗位越低，越暗的组织（脂肪、空气）被显示出来。

**代码字段**：`window_level`

### 4.5 CT 值（Hounsfield Unit / HU）

**定义**：CT 图像中每个像素的密度值，以 Hounsfield（亨斯菲尔德）为单位，水的 CT 值定义为 0 HU。

**参考值**：

| 组织 | CT 值（HU） |
|------|-------------|
| 空气 | -1000 |
| 脂肪 | -100 ~ -50 |
| 水 | 0 |
| 软组织 | 20 ~ 80 |
| 增强血管 | 100 ~ 300 |
| 骨皮质 | 700 ~ 3000 |
| 金属 | >3000 |

### 4.6 图像质量与常见伪影

**图像质量常用概念**：
- **噪声（Noise）**：图像颗粒感，通常随剂量降低、层厚变薄、患者体型增大而增加。
- **空间分辨率（Spatial Resolution）**：区分细小结构的能力，受探测器、重建矩阵、FOV、kernel、层厚影响。
- **低对比度分辨率（Low-Contrast Resolution）**：区分密度差很小组织的能力，常受噪声影响。
- **部分容积效应（Partial Volume Effect）**：一个体素内混合多种组织时，CT 值被平均，可能掩盖小病变。

**常见伪影**：
- **运动伪影（Motion Artifact）**：患者移动、呼吸、心跳导致边缘模糊或重影。
- **金属伪影（Metal Artifact）**：金属植入物造成条纹、暗带或局部 CT 值失真。
- **束硬化伪影（Beam Hardening Artifact）**：低能 X 射线被优先吸收后造成杯状伪影或条纹。
- **环形伪影（Ring Artifact）**：探测器通道校准异常或漂移造成同心圆伪影。

UI 文案应避免把伪影原因写死。例如“图像质量不佳”比“患者移动导致失败”更稳妥，除非系统确实有运动检测证据。

---

## 五、剂量参数

### 5.1 CTDIvol（CT 剂量指数）

**全称**：Volume CT Dose Index，体积 CT 剂量指数  
**单位**：mGy（毫戈瑞）

**定义**：衡量 CT 扫描在扫描区域内的平均辐射剂量，标准化于 16 cm 或 32 cm 模体（Phantom）。

**临床意义**：
- CTDIvol 是国际通用的剂量评估指标
- 医院需根据诊断参考水平（DRL）监控 CTDIvol
- CTDIvol 描述的是标准模体中的扫描输出指标，不等同于某个具体患者或器官实际吸收剂量
- 成人头颅扫描典型值：约 50~70 mGy
- 成人胸部扫描典型值：约 5~15 mGy
- 腹部扫描典型值：约 10~20 mGy

**代码字段**：`ctdi_vol`

### 5.2 DLP（剂量长度乘积）

**全称**：Dose Length Product，剂量长度乘积  
**单位**：mGy·cm

**公式**：`DLP = CTDIvol × 扫描长度(cm)`

**临床意义**：
- 反映整个扫描检查的总辐射剂量（同时考虑了扫描范围）
- 可用于估算有效剂量（Effective Dose），反映患者全身辐射风险
- 有效剂量（mSv）≈ DLP × 转换系数（不同部位系数不同）
- DLP 仍是基于标准模体和扫描长度的指标，不应直接写成“患者实际接受剂量”

**代码字段**：`dlp`

### 5.2.1 SSDE（Size-Specific Dose Estimate，体型特异性剂量估计）

**定义**：SSDE 是在 CTDIvol 基础上结合患者体型（如有效直径或水等效直径）得到的剂量估计，用于比 CTDIvol 更接近个体患者体型影响。

**项目边界**：
- 当前项目没有真实定位像测量、体型自动分割或医学物理剂量引擎，不应声称能计算真实 SSDE。
- 若后续 UI 出现 SSDE，应标注“估算”，并说明依赖患者体型测量和设备剂量报告。
- 儿童和小体型患者尤其不能把 CTDIvol 直接当作患者吸收剂量，因为同一 CTDIvol 对不同体型的剂量意义不同。

### 5.3 DRL（诊断参考水平）

**全称**：Diagnostic Reference Level，诊断参考水平

**定义**：由卫生监管机构或医疗机构制定的剂量参考值，用于判断某类检查的剂量是否在合理范围内。

**重要说明**：DRL 不是剂量限制，是参考值。超过 DRL 不代表违规，但需要审查是否合理。

**项目中的用途**：协议剂量页面有"一键应用系统 DRL"功能，可快速将通知阈值设置为系统 DRL 值。

**UI 文案要求**：
- 应写“超过参考水平 / 需确认 / 建议复核协议”，不要写“违规”或“禁止扫描”。
- DRL 通常用于一类检查的典型剂量监测，不是对单个患者、单次检查的硬性上限。
- 比 DRL 低也不必然代表更好；若图像质量不足，低剂量同样不可接受。

### 5.4 DOM（Dynamic Organ Dose Modulation，动态器官剂量调节）

**完整名称**：Dynamic Organ Dose Modulation / Organ Dose Modulation  
**中文名**：器官剂量保护 / 器官剂量调节

**定义**：在扫描过程中，根据人体解剖方向、保护器官位置和扫描角度，对特定角度或区域的管电流进行降低，减少辐射敏感器官的受照剂量。

**与 4D 动态扫描的区别**：
- DOM：是一种**剂量调节策略**，属于辐射防护概念
- 4D 扫描：是一种**扫描采集模式**，与时相/时间维度相关
- 两者完全不同，**严禁混淆**，UI 文案不能写"DOM（动态扫描）"

**保护器官选项**：
- `breast`：乳腺（胸部扫描，前方）
- `eye_lens`：眼晶状体（头部扫描，前方）
- `thyroid`：甲状腺（颈部扫描，前方）
- `gonad`：性腺（盆腔扫描）

**DOM 开关枚举值**（代码规范）：
```typescript
type DomMode = "off" | "auto" | "breast" | "eye_lens" | "thyroid" | "gonad" | "custom"
```

**控制端职责边界**：
- ✅ 应该：提供配置入口、展示保护状态、给出扫描前确认、记录剂量日志
- ❌ 不应该：在 UI 层计算真实 mA 角度调制曲线；承诺"绝对降低多少剂量"（只能显示"预计/参考"）

**代码字段**：`dom`（位于 `helical_params`、`axial_params`、`topogram_params` 及其 session 副本中）

### 5.5 自动管电流调制（AEC / Auto mA）

**全称**：Automatic Exposure Control，自动曝光控制

**定义**：系统根据患者体型（从定位像分析）或实时衰减信息，在设定的 mA 范围（min_ma ~ max_ma）内自动调整管电流，以在保证图像质量的同时最小化辐射剂量。

**与 DOM 的关系**：DOM 通常需要与 auto mA 联动。如果 auto mA 未启用，DOM 的效果可能受限（因为需要角度级别的 mA 调制能力）。

### 5.5.1 智能剂量调节（Smart Dose Modulation）

**定义**：智能剂量调节是控制端对 CT 辐射剂量优化能力的总称，不等同于单一 DOM 功能。

**功能分层**：
- **一期：定位像衰减驱动的 Z 轴 Auto mA / Z 轴自动管电流调制**。定位像完成后，系统基于定位像沿 Z 轴的衰减/体型估计和断层/螺旋扫描框选范围生成 `position-mA` 曲线，横坐标为患者体轴方向位置或床位位置，纵坐标为管电流 mA。协议提供图像质量目标、调节强度和 mA 安全上下限，技师可在安全边界内调整曲线或 mA 范围，并查看预计剂量变化。
- **二期：DOM 器官剂量保护**。在 Auto mA 基础上，结合体位、保护器官、扫描方向和角度/区域策略，对敏感器官方向进行剂量调节。
- **远期：AI 自动调节扩展**。基于定位像、患者信息、历史协议和图像质量反馈，进行协议推荐、体型识别、剂量预测、风险提示和参数优化建议。

**命名规范**：
- 不把 DOM 写成“智能电流调节”的总称。
- 不把 Auto mA 写成“器官剂量保护”。
- UI 总入口可命名为“智能剂量调节”；一期功能文案应聚焦“自动 mA / 自动管电流调制”。

**一期交互边界**：
- `position-mA` 曲线的编辑容器可以是弹窗、侧边抽屉或页面内面板，具体 UI 形态由后续交互设计决定。
- 无论采用哪种容器，都必须体现定位像框选范围、Z 轴位置、定位像衰减/体型趋势、电流曲线、安全上下限和预计剂量变化。
- 原型可模拟“定位像衰减/体型估计 → position-mA 曲线”的行业逻辑，但不能声称等同真实设备曝光控制、真实剂量引擎或真实患者剂量计算。
- 一期 UI 主视图只展示最终 `position-mA` 电流曲线，不单独展示衰减曲线或体厚曲线，避免把模拟衰减趋势误解为可诊断、可定量数据。
- 一期曲线编辑采用 `min_ma / max_ma` 上下限调节：用户调整安全上下限后，系统在范围内重新生成或压缩 `position-mA` 曲线。曲线可预览，但不支持逐点拖拽编辑。
- 协议模板层只保存默认 Auto mA 开关、默认 mA 上下限和默认调制策略；扫描会话层保存本次患者基于定位像和框选范围生成的 `position-mA` 曲线、本次 Auto mA 开关和本次 mA 上下限。会话内曲线不得反写协议模板。
- 一期 `position-mA` 曲线粒度按公司硬件能力定义为“一个床位位置对应一个 mA 值”。产品需求和数据结构应表达为按床位/table position 的电流序列，而不是任意固定采样点或自由曲线。
- 床位位置序列由定位像完成后的扫描范围选择和扫描计划生成：用户在定位像上确认断层/螺旋扫描起止范围后，系统即可得到本次扫描覆盖的 table positions，并据此生成每个床位位置的 mA 值。UI 可显示平滑曲线，但底层语义是床位位置到 mA 的映射。
- 曲线生成触发逻辑：选择扫描范围后，如果协议默认 `auto_ma = true`，系统自动生成本次 `position-mA` 曲线；如果协议默认 `auto_ma = false`，则用户打开 Auto mA 开关后再生成曲线。
- 扫描范围变化后，table positions 和 `position-mA` 曲线必须自动重新生成；若用户已调整 `min_ma / max_ma`，重新生成时保留用户设置的上下限。若新范围导致上下限或预计剂量不适合，应显示安全提示并要求确认。
- 一期 Auto mA 同时覆盖螺旋扫描和断层/轴扫。螺旋扫描可显示为连续或平滑的 `position-mA` 曲线；断层/轴扫应显示为按离散床位位置变化的阶梯式 `table_position-mA` 曲线。两者使用同一“床位位置到 mA”的语义。
- 涉及曝光控制、床位、电流、球管、机架等硬件相关需求时，需求分析流程必须先说明竞品/行业通常做法，再由硬件团队确认本公司设备能力，最后进入产品定义。

### 5.6 剂量展示与日志规则

剂量相关 UI 必须同时满足“可读”和“不误导”：
- CTDIvol 单位固定为 `mGy`，DLP 单位固定为 `mGy·cm`，有效剂量单位为 `mSv`。
- 协议编辑阶段显示“预计 / 参考”剂量；扫描完成后才可显示“本次记录 / 设备报告值”。
- 多序列检查应区分单序列剂量和检查总剂量；增强多期和 4D 不应只显示一个未说明来源的 DLP。
- 定位像可显示估算剂量，但在项目规则中是否计入总 DLP 必须明确。若不计入，应标注“定位像估算，仅参考”。
- 剂量日志应记录协议名、部位、年龄组、扫描模式、序列类型、CTDIvol、DLP、DOM 状态、确认人和确认时间。

---

## 六、对比剂（Contrast Agent）

### 对比剂是什么

CT 对比剂（又称造影剂）是一种含碘的化学物质，静脉注射后经血液循环进入组织，使血管和某些器官在 CT 图像上显示更高的密度（更亮），便于区分正常与病变组织。

**扫描类型**：
- **平扫（Plain Scan）**：不注射对比剂，直接扫描
- **增强扫描（Contrast-Enhanced Scan）**：注射对比剂后扫描
- **多期增强**：动脉期（约 25~35 秒）、静脉期（约 60~75 秒）、平衡期（约 120~180 秒）

### 关键参数

| 参数 | 说明 | 典型值 |
|------|------|--------|
| 对比剂浓度 | 碘含量（mgI/mL） | 270、300、350、370 mgI/mL |
| 注射速率 | 注射泵速度（mL/s） | 2~5 mL/s |
| 注射总量 | 对比剂用量（mL） | 60~100 mL（成人） |
| 生理盐水冲洗量 | 注射后冲管（mL） | 20~40 mL |
| 触发延迟 | 从注射到扫描开始的等待时间（秒） | 20~30 秒（腹部动脉期） |

**代码对应**：`ContrastConfig`、`ScanSessionContrastConfig`

### 对比剂安全边界（UI 原型）

含碘对比剂使用涉及过敏样反应、肾功能、甲状腺疾病、用药史和静脉通路等临床判断。本项目不做临床决策，但 UI 文案不能忽略这些风险。

**原型中可以做**：
- 展示“增强扫描需确认对比剂风险评估已完成”。
- 记录是否已确认过敏史、肾功能、禁忌证、静脉通路和注射泵连接状态。
- 对儿童、老年、肾功能异常、既往对比剂反应等场景显示“需人工确认”。

**原型中不应做**：
- 自动判断患者一定可以使用对比剂。
- 给出具体给药医嘱或替代治疗建议。
- 用“安全 / 无风险”描述对比剂注射。

---

## 七、患者体位

### 进床方向

| 英文缩写 | 全称 | 中文 | 说明 |
|---------|------|------|------|
| HFS | Head First Supine | 头先进仰卧位 | 最常见体位，患者仰卧，头部先进入机架 |
| HFP | Head First Prone | 头先进俯卧位 | 患者俯卧，头部先进 |
| FFS | Feet First Supine | 脚先进仰卧位 | 患者仰卧，脚先进入机架，用于下肢扫描 |
| FFP | Feet First Prone | 脚先进俯卧位 | 患者俯卧，脚先进 |

### 体位与 DOM 的关系

DOM 的保护方向（前方/后方/左/右）依赖于体位：
- HFS（仰卧）：前方（Anterior）= 患者腹侧
- HFP（俯卧）：前方（Anterior）实际在患者背侧

因此 DOM 配置时必须结合 `position` 字段正确判断保护方向。

---

## 八、扫描部位

| 英文代码 | 中文 | 典型协议示例 |
|---------|------|------------|
| HEAD | 头部 | 脑部平扫、鼻窦、内听道、眼眶、垂体、脑血管 CTA |
| NECK | 颈部 | 颈部软组织、颈椎、喉部、甲状腺 |
| CHEST | 胸部 | 胸部常规、低剂量肺癌筛查、肺动脉 CTA、心脏 CTA、4D 胸部 |
| SPINE | 脊柱 | 颈椎、胸椎、腰椎、骶椎 |
| ABDOMEN | 腹部 | 腹部平扫/增强、肝脏三期、胰腺期、肾脏、腹主动脉 CTA |
| PELVIS | 盆腔 | 盆腔平扫/增强、下腹部、膀胱 |
| EXTREMITY | 四肢 | 膝关节、腕关节、足、髋关节 |

### 解剖部位与辐射敏感器官的对应

| 扫描部位 | 辐射敏感器官 | DOM 推荐 |
|---------|------------|---------|
| HEAD | 眼晶状体 | eye_lens |
| NECK | 甲状腺 | thyroid |
| CHEST | 乳腺（尤其女性和儿童） | breast |
| PELVIS | 性腺 | gonad |

---

## 九、DICOM 标准

**DICOM**（Digital Imaging and Communications in Medicine）是医学影像传输与存储的国际标准，定义了图像格式、元数据结构和网络传输协议。

### 关键概念

- **DICOM 文件**：扩展名 `.dcm`，包含图像像素数据和大量元数据（患者信息、扫描参数、设备信息等）
- **DICOM Tag**：元数据的标识符，格式如 `(0010,0010)` 表示患者姓名
- **Series（序列）**：一次扫描采集的图像集合，对应一组连续的横断面图像
- **Study（检查）**：一次完整检查，可包含多个 Series
- **Instance（实例）**：单张 DICOM 图像

### CT 常用 DICOM 标签（项目可读字段）

| Tag | 名称 | 用途 |
|-----|------|------|
| `(0010,0010)` | Patient Name | 患者姓名，原型展示时注意脱敏 |
| `(0010,0020)` | Patient ID | 患者 ID |
| `(0020,000D)` | Study Instance UID | 检查唯一标识 |
| `(0020,000E)` | Series Instance UID | 序列唯一标识 |
| `(0008,0060)` | Modality | 设备类型，CT 图像通常为 `CT` |
| `(0018,0060)` | KVP | 管电压 |
| `(0018,1151)` | X-Ray Tube Current | 管电流 |
| `(0018,9305)` | Revolution Time | 机架旋转时间 |
| `(0018,9311)` | Spiral Pitch Factor | 螺距 |
| `(0018,0050)` | Slice Thickness | 层厚 |
| `(0028,1050)` | Window Center | 窗位 |
| `(0028,1051)` | Window Width | 窗宽 |
| `(0020,0032)` | Image Position Patient | 图像在患者坐标系中的位置 |
| `(0020,0037)` | Image Orientation Patient | 图像方向余弦，用于 MPR 排列 |

注意：DICOM 标签是否存在、单位和含义可能受设备、增强对象类型、私有标签和导出方式影响。解析失败时 UI 应显示“缺失 / 未提供”，不要猜测。

### MPR（Multi-Planar Reconstruction，多平面重建）

**定义**：将轴位（横断面）图像数据集重建为冠状面（Coronal）、矢状面（Sagittal）或任意斜面的图像。

**项目对应**：`CornerstoneMPRViewport.tsx`，路由 `/image-viewer` 支持 MPR 模式。

### 项目中的 DICOM 实现

- **Cornerstone3D**：项目使用的 DICOM 渲染库（`@cornerstonejs/core`、`tools`、`dicom-image-loader`）
- **初始化**：`lib/cornerstone/initCornerstone.ts`
- **Stack 视图**：逐张浏览单个序列图像（`CornerstoneStackViewport.tsx`）
- **MPR 视图**：同时显示轴位/冠状位/矢状位（`CornerstoneMPRViewport.tsx`）
- **MHA 格式**：用于 4D CT 体积数据的存储格式，项目中有 `lib/mhaImageLoader.ts` 处理

---

## 十、服务模式（Service Mode）功能说明

### 球管预热（Tube Warmup）

**背景**：CT 球管（X 射线管）在长时间不使用（如隔夜）后，阳极靶面温度降低。直接用高参数扫描可能因热冲击损坏球管。

**预热过程**：按照预设程序，从低 kV/mA 开始，逐步增加到正常工作参数，使球管阳极逐渐升温，整个过程约 5~30 分钟。

**触发条件**：开机后、设备长时间空置后、球管更换后。

### 空气校准（Air Calibration）

**背景**：CT 探测器的增益和偏移会随时间漂移，导致图像出现环形伪影（Ring Artifact）。

**校准过程**：在扫描孔内无患者的情况下进行空气扫描，系统采集纯空气的探测器响应并更新校准数据。

**触发条件**：每日开机后、机房温度变化较大时、图像出现明显环形伪影时。

### 日检（Daily QA / Daily Quality Assurance）

**定义**：每日开始工作前对 CT 设备进行的常规质量检测，确认设备性能在标准范围内。

**典型检测项目**：球管预热状态、空气校准、噪声水平、CT 值准确性（水模）、空间分辨率、低对比度分辨率。

### QA 报告（QA Report）

**定义**：记录设备质量检测结果的报告，供质量管理和监管审查使用。包含各检测项目的测量值、合格/不合格状态和历史趋势。

### 角点信息（Corner Info）

**背景**：CT 机架有多个关键位置（角点），需要记录坐标用于设备安装调校和维护参考。

---

## 十一、扫描工作流状态机

项目前端通过 `lib/scanWorkflowSession.ts` 管理跨页面的工作流状态，核心概念：

- **ScanSession**：一次完整的扫描检查实例，从选择协议开始到图像查看结束
- **WorkflowPlan**：当前检查包含哪些序列（Series），按顺序执行
- **CurrentSeries**：当前正在进行的序列（定位像/螺旋/4D等）

### 状态持久化机制

| 存储方式 | 用途 | 生命周期 |
|---------|------|---------|
| localStorage | 当前患者信息、扫描会话 ID、工作流计划 | 页面刷新后保留 |
| Dexie（IndexedDB） | 协议模板快照 | 长期缓存 |
| React State | 当前页面 UI 状态 | 页面卸载后丢失 |
| 后端 SQLite | 扫描会话、参数、患者数据 | 永久保存 |

---

## 十二、竞品与行业背景

本项目为 CT 控制端原型，参考行业主流厂商：

| 厂商 | 代表产品/技术 |
|------|------------|
| Siemens Healthineers | SOMATOM 系列，CARE Dose4D（自动剂量调节），器官保护功能 |
| GE Healthcare | Revolution 系列，Auto mA（噪声指数导向），Smart mA |
| Philips | Ingenuity/Incisive 系列，DoseWise（剂量管理体系） |
| Canon Medical | Aquilion 系列，SUREExposure（自动曝光控制） |
| 联影（United Imaging） | uCT 系列，国产主流 CT |

**重要说明**：本项目是原型验证工具，非临床设备，不需要满足医疗设备注册要求。但所有参数边界、默认值、UI 文案应尽量符合临床实际，避免误导产品方向。

---

## 十三、术语速查表

| 术语 | 解释 |
|------|------|
| Scout / Topogram / Scanogram | 定位像，正式扫描前的低剂量导引图像 |
| Helical / Spiral Scan | 螺旋扫描，床进机转的主要扫描模式 |
| Axial Scan | 轴扫，逐层间歇采集 |
| 4D CT | 四维 CT，含时间维度的呼吸门控扫描 |
| Phase（时相） | 4D 扫描中对应某一呼吸状态的图像集 |
| Pitch（螺距） | 机架转一圈床进距离与探测器宽度的比值 |
| kV（管电压） | 决定 X 射线穿透力 |
| mA（管电流） | 决定 X 射线数量（光子数） |
| mAs（毫安秒） | mA × 旋转时间，每层总曝光量 |
| Auto mA / AEC | 自动管电流调制，根据体型自动调整 mA |
| FOV（扫描视野） | 图像覆盖的圆形区域直径（mm） |
| Slice Thickness（层厚） | 每层图像的 Z 轴厚度（mm） |
| Slice Interval（层间距） | 相邻层中心间距（mm） |
| Kernel（滤波函数） | 决定图像锐利度和噪声的重建算法 |
| WW（窗宽） | 图像显示的 HU 范围跨度 |
| WL（窗位） | 图像显示的 HU 范围中心值 |
| HU（CT 值） | 组织密度的 Hounsfield 单位 |
| CTDIvol | CT 剂量指数（mGy），扫描区域平均剂量 |
| DLP | 剂量长度乘积（mGy·cm），总剂量指标 |
| DRL | 诊断参考水平，机构制定的剂量参考标准 |
| DOM | 动态器官剂量调节，对敏感器官方向降低 mA |
| MPR | 多平面重建，轴位数据重建为冠/矢状面 |
| DICOM | 医学影像传输存储国际标准 |
| HFS | Head First Supine，头先进仰卧位（最常见） |
| FFS | Feet First Supine，脚先进仰卧位 |
| AEC | Automatic Exposure Control，自动曝光控制 |
| Gantry（机架） | CT 球管和探测器旋转的圆形结构 |
| Bore（扫描孔） | 患者进入的机架中心圆孔 |
| Contrast Agent | 对比剂/造影剂，含碘静脉注射增强显示 |
| Plain Scan | 平扫，不使用对比剂 |
| Contrast-Enhanced Scan | 增强扫描，注射对比剂后扫描 |
| Recon（重建） | 图像重建，将采集数据计算为可视图像 |
| Raw Data | 原始数据，探测器采集的未重建数据 |
| Ring Artifact | 环形伪影，探测器漂移导致的图像缺陷 |
| Tube Warmup | 球管预热，防止冷球管热冲击损坏 |
| Air Calibration | 空气校准，消除环形伪影的探测器校准 |
| Daily QA | 日检，每日开机质量检测 |
| Series（序列） | 一次采集的图像集合（含定位像/螺旋/重建各序列） |
| Study（检查） | 一次完整检查，包含多个 Series |
| Breathing Waveform | 呼吸波形，4D 扫描时同步记录的呼吸信号 |
| Peak/Valley（峰/谷） | 呼吸波形的高点（吸气末）/低点（呼气末） |
| Rescan（重扫） | 4D 扫描后对质量差的时相进行补充扫描 |

---

## 十四、参考来源与使用原则

本知识库优先采用稳定、公开、权威的医学影像资料作为概念依据，包括：
- FDA：[Computed Tomography (CT)](https://www.fda.gov/Radiation-EmittingProducts/RadiationEmittingProductsandProcedures/MedicalImaging/MedicalX-Rays/ucm115317.htm)、[Radiation Quantities and Units](https://www.fda.gov/radiation-emitting-products/medical-x-ray-imaging/radiation-quantities-and-units)，用于 CT 基本原理、辐射剂量量纲、CT 风险沟通。
- IAEA：[Diagnostic Reference Levels](https://www.iaea.org/resources/rpop/health-professionals/radiology/diagnostic-reference-levels)，用于 DRL 的定义、用途和“不是单个患者硬性限值”的原则。
- AAPM：[Report 096](https://www.aapm.org/pubs/reports/detail.asp?docid=97)、[Report 204](https://www.aapm.org/pubs/reports/detail.asp?docid=143)、[Report 220](https://www.aapm.org/pubs/reports/detail.asp?docid=146)，用于 CTDIvol、DLP、SSDE 等 CT 剂量指标的物理含义和局限。
- DICOM Standard / NEMA：[DICOM Standard](https://www.dicomstandard.org/)，用于 DICOM 信息对象、Study / Series / Instance 与 CT 元数据标签。
- Image Gently：[CT Scan Protocols for Children](https://www.imagegently.org/Procedures/Computed-Tomography/CT-Scan-Protocols-for-Children)，用于儿童 CT 协议应按体型和临床目的调整的原则。

使用本文件时遵循：
- 能写“参考/预计/需确认”的地方，不写“真实/保证/绝对”。
- 能用字段判断的地方，不用名称猜测，例如 4D 必须用 `acquisition_type === "4d"`。
- 能保留设备差异的地方，不写成所有厂商通用结论。

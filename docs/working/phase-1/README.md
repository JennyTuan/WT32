---
type: phase-1-working-index
project: WT32
status: working
created: 2026-07-16
updated: 2026-07-16
decision_baseline: ../../baseline/2026-07-16-WT32-阶段0产品基线.md
historical_plan: ../../archive/2026-07-16-WT32-整体项目优化计划.md
---

# WT32 阶段 1 工作区

> 本目录记录阶段 1“统一状态、数据与异常恢复”的当前实施证据。它不是新的产品批准记录，也不把自动化测试通过等同于完整验收通过。正式目标仍以 [WT32 阶段 0 产品基线](../../baseline/2026-07-16-WT32-阶段0产品基线.md) 为准，整体先后顺序可追溯到 [WT32 整体项目优化计划](../../archive/2026-07-16-WT32-整体项目优化计划.md)。

> WT32 仍是产品与 UI 验证原型。扫描、呼吸、影像和剂量相关数据均为模拟、预计、参考或需确认信息，不得解释为真实设备控制、诊断结论、最终剂量计算或安全批准。

## 1. 当前合并态

| 项目 | 状态 | 当前结论 |
| --- | --- | --- |
| D0-002 独立恢复动作 | **已实现，待浏览器验证** | `return_to_edit`、`retry_series`、`terminate_exam` 已通过统一接口、动作幂等键、动作台账和序列尝试记录落地；`finish_with_partial` 在正式部分结果模型完成前保持明确 `409` 不可用，且无副作用。 |
| D0-003 常规轴扫正式执行 | **已实现，待浏览器验证** | 常规轴扫已绑定患者、会话、目标序列和前置定位像，按受控状态机完成模拟执行；参数写入会在确认范围和进入执行前刷新。 |
| D0-004 完成权威时点 | **已实现，待浏览器验证** | 普通会话只能在所有计划序列均为 `image_ready` 时由后端完成；查看器只退出查看。4D 使用独立原子完成接口。 |
| D0-005 4D 结果持久化与恢复 | **已实现，待端到端与目标库验证** | 4D 结果、来源、版本、尝试记录和阶段迁移已持久化；最终结果、目标序列、会话与尝试在同一事务内完成，刷新和历史入口重新读取权威状态。 |
| D0-006 影像来源关联 | **已实现，待浏览器验证** | 非 4D 序列使用按序列登记的 v1 影像来源；4D 使用结果级来源。查看器校验患者、会话、序列、来源和版本，缺失或不匹配时失败关闭，不挂载固定演示兜底。 |
| 参数与范围一致性 | **已实现，待浏览器验证** | 确认页在继续前刷新防抖参数写入；后端在参数变化后使旧的范围确认失效，后续模拟扫描必须重新确认。 |
| RespiraScope 兼容模拟服务 | **已实现，待浏览器与局域网验证** | 本地 `/health`、`/startReceive` 和 Engine.IO WebSocket 模拟契约已经落地并纳入后端测试；所有响应和事件均标识为模拟。 |

当前状态不引入新的产品决策。D0-001 至 D0-009 已全部批准；阶段 1 仍保持 `working`，因为浏览器黄金流程、目标 PostgreSQL 测试库和局域网设备连通尚无通过证据。

## 2. 当前质量证据

- 后端全量测试：`88/88` 通过。
- 前端测试：`13 files / 104 tests` 通过。
- ESLint：通过，无错误。
- TypeScript：`tsc -b --pretty false` 通过。
- 前端生产构建：通过，用时约 `3m14s`。
- Alembic：单一迁移头 `20260716_0006`。
- `git diff --check`：仅换行符转换提示，没有空白错误。
- 构建仍有既有警告：Cornerstone 依赖的 `fs` / `path` 浏览器 externalize 提示，以及大于 500 kB 的 chunk 提示。它们不阻塞本次构建，但必须在后续性能与构建治理中处理。

这些证据只覆盖当前本地合并态，不代表以下项目已经通过：1024 × 768 浏览器黄金流程、局域网平板访问、真实 RespiraScope 设备、目标 PostgreSQL 实库升级/回滚。

## 3. 本目录文档

- [WT32 阶段 1 执行记录](WT32_PHASE1_EXECUTION_LOG.md)：D0-002 至 D0-006、参数一致性、RespiraScope 模拟服务、质量证据和剩余验收项。

## 4. 接下来按此顺序收口

1. 在 1024 × 768 下执行常规螺旋、常规轴扫、门控、4D 和已完成检查查看五条浏览器黄金流程，覆盖刷新、失败、重试、终止和关联缺失。
2. 在目标 PostgreSQL 测试库验证迁移升级、当前迁移头、约束和回滚；不得使用真实患者数据。
3. 验证同机 RespiraScope 模拟波形，再验证可信局域网平板的连通、断连、重连和超时；这不等于真机接入。
4. 将 Cornerstone externalize 和大 chunk 警告纳入后续构建与性能优化，不在阶段 1 文档中误写为“无警告”。
5. 只有上述运行证据齐全且原型边界仍清晰时，才把阶段 1 从 `working` 改为 `complete`。
